export type ExhibitData = {
  number: string;
  title: string;
  kind: string;
  description: string;
  background: string;
  coreUx: string;
  metrics: string[];
  stack: string[];
  demoUrl?: string;
  githubUrl?: string;
  image?: string;
};
